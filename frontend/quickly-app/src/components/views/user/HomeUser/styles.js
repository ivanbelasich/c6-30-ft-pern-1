import { StyleSheet } from "react-native";

// Styles HomeUser

const styles = StyleSheet.create({
  color: {
    color: "red",
    fontWeight: 700,
  },
  container: {
    borderRadius: 15,
    borderWidth: 1,
    marginBottom: 10,
  },
});

export default styles;
