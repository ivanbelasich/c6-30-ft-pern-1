// Register styles
import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  imageBackground: {
    flex: 1,
  },
  logo: {
    width: "100%",
    marginTop: 20,
  },
});

export default styles;
