import { View, Text } from "react-native";
import FilterBar from "../../FilterBar/FilterBar";
import styles from "./style";

const Home = () => {
  return (
    <View>
      <FilterBar />
    </View>
  );
};

export default Home;
